import { before, describe } from "mocha";

suite("files", () => {
    before(() => {
        console.log("Starting files tests");
    });

    describe("Copy file path", () => {
        test("Copy file path", () => {
            
        });
    });
});